# Dagster core (all pinned to same release line)
dagster==1.12.15
dagster-webserver==1.12.15
dagster-postgres==0.28.15
dagster-docker==0.28.15
dagster-aws==0.28.15

# Dagster integrations
dagster-dbt==0.28.15
dagster-dlt==0.28.15

# dbt
dbt-core>=1.8
dbt-postgres>=1.8
dbt-redshift>=1.8

# dlt
# Keep only the database extras used by this stack to avoid pulling in
# filesystem/s3fs pins that conflict during image builds.
dlt[postgres,redshift]==1.23.0
s3fs>=2024.6.0

# AWS & data
boto3>=1.34
redshift-connector>=2.1
sqlalchemy>=2.0
psycopg2-binary>=2.9
pandas>=2.0
python-dotenv>=1.0
pydantic-settings>=2.0
